DOKUMENTASI SETUP PROJECT TRACER STUDY

- Tujuan Project: Project ini untuk membuat Sistem Informasi Tracer Study Alumni (SERU).
- Backend: Laravel digunakan sebagai backend framework.
- Frontend: Vue 3 digunakan sebagai frontend framework (menggantikan Blade).
- Bridge: Inertia.js digunakan sebagai bridge antara Laravel dan Vue, memungkinkan SPA (Single Page Application) tanpa perlu membuat API secara terpisah.
- Styling: Tailwind CSS digunakan untuk styling.
- Bundler: Vite digunakan untuk asset bundling dan development.
- Database: Database menggunakan MySQL/MariaDB.
- Local Environment: Local environment menggunakan Laragon.
- Lokasi Project: c:\ALL FILES\Tugas Akhir\tracer\tracer-study

CARA MENJALANKAN PROJECT
1. Buka dua terminal.
2. Di terminal pertama, jalankan backend Laravel: php artisan serve
3. Di terminal kedua, jalankan Vite development server: npm run dev
4. Akses http://localhost:8000 di browser.

FUNGSI FOLDER UTAMA LARAVEL
- app/: Berisi inti kode aplikasi seperti Controller, Model, dan Middleware.
- bootstrap/: Berisi script untuk inisialisasi aplikasi (contoh: app.php).
- config/: Berisi semua file konfigurasi aplikasi.
- database/: Berisi migration, seeder, dan factory untuk database.
- public/: Folder root untuk web server, berisi file entry index.php serta aset yang sudah dicompile.
- resources/: Berisi raw asset seperti view Blade (app.blade.php), CSS, dan file Javascript/Vue.
- routes/: Berisi definisi routing aplikasi (contoh: web.php).
- storage/: Berisi file yang digenerate oleh Laravel seperti log, cache, dan file upload.
- vendor/: Berisi dependensi Composer.

FUNGSI FOLDER resources/js
Folder ini merupakan pusat pengembangan frontend Vue dalam arsitektur Inertia.
- app.js: Entry point utama untuk inisialisasi Vue dan Inertia.
- Pages/: Berisi komponen Vue yang merepresentasikan halaman secara penuh (dirender oleh controller Laravel).
- Layouts/: (Opsional) Berisi komponen layout untuk membungkus halaman.
- Components/: (Opsional) Berisi reusable komponen Vue yang lebih kecil.

ARSITEKTUR DATABASE (STEP 3)
1. Tabel `users`:
   - Menyimpan kredensial autentikasi utama.
   - Field: id, name, username (unik), email (nullable), role, must_change_password, password.
   - Relasi: hasOne(Biodata::class)
2. Tabel `biodatas`:
   - Menyimpan data spesifik profil biodata alumni (28 field profil lengkap + karier).
   - Field: id, user_id (FK), nim, tahun_lulus, kode_prodi, prodi_id, nama, nomor_telepon, email, email_pribadi, alamat, kabupaten_id, provinsi_id, kelurahan, kecamatan, kode_pos, agama, nik, no_kk, no_bpjs, npwp, instagram_url, facebook_url, linkedin_url, linkedin_username, expert, minat, company_id, atasan_id, posisi_jabatan, jenis_pekerjaan, zipcode.
   - Relasi: belongsTo(User::class), hasOne(DataAkademik::class), hasOne(Yudisium::class), hasOne(DataOrangTua::class), belongsTo(Prodi::class), belongsTo(Company::class), belongsTo(Atasan::class), hasMany(Tracer::class), hasMany(ProdiResponse::class).

MANAJEMEN OTORISASI (ROLES)
- Terdapat 4 peran utama:
  1. `superadmin`: Otoritas tertinggi sistem, bertanggung jawab penuh atas konfigurasi sistem serta modul Kelola Kuesioner (manajemen butir pertanyaan F1-F22, alur jump logic, dan opsi jawaban).
  2. `admin_biro3`: Administrator Biro 3 (Kemahasiswaan, Alumni & Pengembangan Karir), bertanggung jawab atas Manajemen Data Alumni, Sinkronisasi Profil LinkedIn, dan Pemantauan Partisipasi Kuesioner.
  3. `admin_prodi`: Administrator Program Studi, memantau capaian responden di tingkat prodi.
  4. `alumni`: Pengguna lulusan yang mengisi kuesioner dan memperbarui biodata/profil karier.
- `RoleMiddleware` digunakan untuk memproteksi akses dashboard dan modul berdasarkan role.
- `CheckMustChangePassword` menahan user (khususnya alumni baru) agar mengganti password sebelum bisa masuk sistem.

ATURAN PENGEMBANGAN (CODING GUIDELINES)
1. **Update Log**: Setiap update atau fitur baru HARUS selalu dicatat ke dalam `UPDATE_LOG.md`.
2. **Komentar Kode**: Setiap perintah code, routes, functions, view, dan logika krusial HARUS memiliki comment penjelasan yang deskriptif di dalam kodenya.
3. **Penamaan Konsisten**: Nama file HARUS disesuaikan dengan fungsinya dengan jelas. Hal ini berlaku untuk nama Controller, nama routes, view, fungsi, variabel, dll.
4. **Pemisahan Tanggung Jawab (Separation of Concerns)**: Semuanya (business logic, query, security, manipulasi data berat) HARUS melalui Backend (Laravel). Frontend (Vue) difungsikan murni HANYA sebagai tampilan (UI/Presentation Layer) saja.

ARSITEKTUR KUESIONER (STEP 4 & 5)
1. Tabel Dinamis (Hierarkis):
   - `questionnaires` -> `question_sections` -> `questions` -> `question_options`
   - Jawaban disimpan di tabel `responses` dengan format kolom `answer_text` (untuk jawaban tunggal) dan `answer_json` (untuk checkbox / matriks).
2. Format Jump Logic (Google Forms Style):
   - Kolom `jump_to` di tabel `question_options` mencatat langsung kode pertanyaan tujuan jika opsi tersebut dipilih (misal: 'Saya tidak mencari kerja' -> jump_to: 'F8').
   - Sistem Vue di halaman kuesioner mendeteksi `jump_to` dari opsi jawaban yang dipilih dan melakukan skip/bypass pertanyaan dan section di antaranya secara dinamis.
   - Jump logic dapat dikelola dengan mudah oleh admin melalui panel Admin Biro Tiga (Kelola Pertanyaan).
3. Manajemen Tipe Data Input:
   - Komponen UI dipetakan murni berdasarkan atribut `type` dari DB: `text`, `number`, `radio`, `checkbox`, `radio_input` (dengan inputan ekstra), `multiple_number`, `matrix`, `matrix_dual`.
4. Scoping Pertanyaan Program Studi:
   - Kolom `prodi_id` di tabel `questions` (nullable foreign key ke `prodis.id`).
   - Jika bernilai NULL, pertanyaan berlaku umum untuk semua program studi.
   - Jika diisi, pertanyaan hanya disajikan untuk alumni prodi terkait (disaring otomatis di `KuesionerController` tanpa hardcode di Vue, misal: F2E untuk prodi Filsafat Keilahian kode 31).
   - Admin Biro 3 dapat memilih target program studi dan memantau statusnya via badge prodi di panel Kelola Pertanyaan.
5. Khusus Evaluasi Kompetensi (F17 / Komparasi Dual Berdampingan):
   - Pasangan pertanyaan berdampingan: Pertanyaan ganjil = Kolom A (Kemampuan Diri Anda / Tingkat Penguasaan), Pertanyaan genap = Kolom B (Kontribusi Perguruan Tinggi).
   - Tampilan menggunakan tabel komparatif dengan sticky header, tema warna kontras (Hijau Emerald untuk Diri vs Biru Royal untuk Kampus), serta indikator komparasi dinamis (Diri > Kampus, Selaras, Kampus > Diri) pada kolom tengah.

PANEL BIRO 3: DIREKTORI ALUMNI & STANDARISASI NAVIGASI (STEP 6)
1. Syarat Tampil Alumni di Biro 3:
   - Alumni HANYA ditampilkan apabila telah berstatus yudisium 'Lulus' (relasi `yudisium` atau `dataAkademik` dengan `proses_yudisium = 'Lulus'`).
   - Query backend di `DaftarAlumniController.php` menyaring langsung dengan `whereHas('yudisium', fn($q) => $q->where('proses_yudisium', 'Lulus'))`.
2. Dropdown Tahun Yudisium & Penyajian Data per Tahun Kelulusan:
   - Data alumni TIDAK ditampilkan sekaligus (semua semester ditumpuk), melainkan disajikan terisolasi per tahun / semester kelulusan yang dipilih.
   - Menggunakan komponen Dropdown "Tahun / Semester Yudisium" (bukan navigasi tab), di mana backend secara default memilih periode kelulusan terbaru.
   - Backend `DaftarAlumniController.php` memfilter secara langsung `whereHas('dataAkademik', fn($q) => $q->where('tahun_akademik_lulus', $semesterTerpilih))`.
   - Di dropdown, opsi dilengkapi dengan indikator jumlah wisudawan untuk periode terkait (contoh: "Gasal 2025/2026 (4 Alumni)").
3. Standarisasi Navigasi & Visual Biro 3:
   - Seluruh halaman di bawah naungan Biro 3 (`/biro3/dashboard`, `/biro3/alumni`, `/biro3/pertanyaan`, `/biro3/alumni/{id}`) menggunakan navbar atas seragam dengan 3 rute menu utama: Dashboard, Data Alumni, dan Kelola Pertanyaan.
   - Skema warna formal mengacu pada Hijau Resmi UKDW (`#005B3C`), Kuning Resmi Landing Page (`#FACC15`), dan Putih/Slate bersih. Dilarang menggunakan warna orange atau aksen merah menyala di area Biro 3.

ARSITEKTUR & STANDARISASI PENYIMPANAN RESPONSES (STEP 7)
1. Struktur Tabel `responses`:
   - `id`: Primary Key (BigInt Unsigned).
   - `alumni_id`: Foreign Key ke `alumnis.id`.
   - `question_id`: Foreign Key ke `questions.id`.
   - `answer_text`: Kolom Text/Varchar untuk representasi teks manusia, format angka tunggal, dan data siap ekspor.
   - `answer_json`: Kolom JSON untuk representasi array asli (multiple choice), rincian nominal (F13), dan input terstruktur (radio_input).
   - `created_at` & `updated_at`: Timestamp.

2. Strategi Penyimpanan Berdasarkan Tipe Pertanyaan (Dual Hybrid Storage):
   - **Teks / String Tunggal** (`text`, `searchable_select`):
     - `answer_text`: String teks murni (contoh: "Jl. Kaliurang Km 12").
     - `answer_json`: NULL.
   - **Angka Numerik Tunggal** (`number`, skala rating `single_choice` 1-5 pada F17):
     - `answer_text`: Angka murni (contoh: "5" pada F6, "2" pada F7, "4" pada F17).
     - `answer_json`: NULL.
     - *Manfaat*: Memungkinkan fungsi agregasi SQL langsung (`AVG()`, `SUM()`) dan terformat otomatis sebagai number di Excel.
   - **Pilihan Tunggal / Radio** (`single_choice`, `radio`):
     - Jika memilih opsi reguler: `answer_text` = opsi yang dipilih (contoh: "Ya", "Swasta"), `answer_json` = NULL.
     - Jika memilih opsi "Lainnya" + teks kustom: `answer_text` = "Lainnya: [isi_teks_alumni]" (contoh: "Lainnya: Merintis usaha"), `answer_json` = NULL.
   - **Pilihan Ganda / Checkbox** (`multiple_choice`, `checkbox`):
     - `answer_json`: Array JSON murni (contoh: `["Melamar ke perusahaan", "Mencari di internet", "Lainnya: Job Fair"]`). Menjaga keutuhan array tanpa takut terpecah oleh tanda koma teks.
     - `answer_text`: String gabungan dipisahkan koma (contoh: "Melamar ke perusahaan, Mencari di internet, Lainnya: Job Fair"). Siap diekspor langsung ke spreadsheet.
   - **Radio Input Kombinasi** (`radio_input`, seperti F3 dan F5):
     - `answer_json`: Object terstruktur `{"selected": "Kira-kira ... bulan sebelum lulus", "input": "4"}`.
     - `answer_text`: Kalimat utuh yang menggantikan titik-titik (contoh: "Kira-kira 4 bulan sebelum lulus").
   - **Rincian Angka / Pendapatan** (`multiple_number`, seperti F13):
     - Format Pengisian: Satuan ribuan rupiah dengan visual suffix `.000` otomatis (contoh: ketik 5000 untuk Rp 5.000.000, 750 untuk Rp 750.000). Dilengkapi peringatan pintar jika nominal diinput dalam jutaan penuh (>= 1.000.000) untuk mencegah kesalahan perkalian ganda.
     - `answer_json`: Object key-value numerik dalam rupiah penuh serta nilai total akumulasi `{"F13-01": 6500000, "F13-02": 750000, "F13-03": 0, "total": 7250000}`.
     - `answer_text`: Kalimat terformat rapi dengan label, rupiah, dan total akumulasi (contoh: "Dari Pekerjaan Utama: Rp 6.500.000, Dari Lembur dan Tips: Rp 750.000, Dari Pekerjaan Lainnya: Rp 0, Total Pendapatan: Rp 7.250.000").
     - Total Salary: Terkalkulasi secara reaktif dan otomatis disinkronisasi/diperbarui di database setiap kali alumni mengubah rincian pendapatan.

3. Integrasi & Sinkronisasi Profil Alumni (F1 s/d F2H):
   - Instrumen profil (F1, F2A, F2B, F2C, F2D, F2D1, F2E, F2E1, F2E2, F2E3, F2F, F2G, F2H) ditarik otomatis dari `data_akademiks`, `alumnis`, `companies`, dan `atasans` melalui `QuestionMapping`.
   - `KuesionerSyncService::syncProfileResponses($alumni)` otomatis mengisi tabel `responses` untuk setiap alumni saat:
     a. Database seeder dijalankan (`QuestionMappingSeeder`).
     b. Kuesioner dibuka oleh alumni (`KuesionerController`).
     c. Alumni mengubah/memperbarui profil diri di `/alumni/profil` (`SimpanProfilController`).
     d. Alumni menyimpan kuesioner (`SimpanJawabanController`).
   - Tabel `responses` terjamin 100% lengkap memiliki data untuk seluruh 23 instrumen tracer study.

4. Panduan Ekspor Data (Excel / CSV):
   - **Ekspor Cepat Laporan Manusia**: Cukup SELECT `answer_text`. Kolom di Excel langsung rapi, terbaca manusia, dan tidak ada tanda kurung siku `[]` atau petik `""`.
   - **Ekspor Statistik Resmi Dikti**: Gunakan `answer_json` untuk memetakan centang opsi ke kolom biner 1/0 dan angka numerik gaji ke kolom nominal terpisah.

ARSITEKTUR PROFIL ALUMNI & DATA PATEN UNIVERSITAS (STEP 8)
1. Struktur Tab Profil Alumni (`/alumni/profile`):
   - **Tab 1: Identitas & Alamat (`FormPribadi.vue`)**:
     - *Identitas Diri*: NIM (🔒 Paten), Nama Lengkap, NIK, NPWP, Tempat/Tgl Lahir, Jenis Kelamin, Agama, Gol. Darah, Kewarganegaraan.
     - *Dokumen Pendukung*: No KK, NISN, No BPJS Kesehatan.
     - *Kontak & Alamat Domisili*: No Telepon/WhatsApp, Email Pribadi, Email Students, Alamat Lengkap, Provinsi, Kabupaten/Kota, Kecamatan, Kelurahan, Kode Pos.
   - **Tab 2: Akademik & Yudisium (`FormAkademik.vue`)**:
     - *Data Paten Resmi Universitas* (🔒 Read-Only): NIM, Angkatan Masuk, Status Mahasiswa ('Lulus'), Tanggal/Semester Kelulusan (`tahun_akademik_lulus`), Tahun Lulus, IPK, Total SKS, dan Total Angka Kualitas.
     - *Skripsi & Publikasi*: Judul TA (Indonesia & Inggris), URL Repositori, Jenis Publikasi, Status Publikasi, Dosen Pembimbing (1, 2, 3), Dosen Penguji (1, 2, 3, 4).
     - *Data Yudisium*: Keterangan Hasil Yudisium / Predikat (Cum Laude, Sangat Memuaskan, Memuaskan), Status Proses Yudisium.
   - **Tab 3: Data Orang Tua (`FormOrangTua.vue`)**:
     - Nama Orang Tua / Wali, Pekerjaan, No Telepon / WhatsApp.
     - Alamat Lengkap, Provinsi, Kabupaten/Kota, Kota, Kode Pos.
   - **Tab 4: Karier & Jejaring (`FormKarier.vue`)**:
      - LinkedIn Profil & Username, Instagram, Facebook.
      - Keahlian (Expertise), Minat (Interest), Posisi Jabatan, Jenis Pekerjaan (Gerejawi / Non Gerejawi).
      - Data Perusahaan: Searchable dropdown perusahaan (dengan fitur pencarian sticky), Provinsi, Kabupaten/Kota, Skala, Alamat, Zipcode/Kode Pos, Status Verifikasi.
      - Popup Modal Tambah Perusahaan: Desain solid clean tanpa blur (`backdrop-blur` ditiadakan), dengan validasi wajib nama perusahaan, provinsi, kabupaten/kota, dan kode pos yang langsung tersimpan ke database (`POST /alumni/company`).
      - Data Atasan Langsung: Nama, Email, Nomor Telepon.

2. Aturan Keamanan Bidang Paten (Patent Field Protection):
   - Di frontend: Komponen input paten disajikan dengan tampilan abu-abu netral profesional (`bg-gray-100 text-gray-700 cursor-not-allowed`) tanpa ikon badge/gembok berlebih (non AI-slop).
   - Di backend: `SimpanProfilController.php` memfilter dan membekukan field paten (`nim`, `angkatan_masuk`, `status_mahasiswa`, `tahun_akademik_lulus`, `tahun_lulus`, `ipk`, `total_sks`, `total_angka_kualitas`, serta seluruh record `yudisiums`) sehingga tidak dapat diubah oleh alumni sekalipun via request injection.

3. Seeder & Skema Perusahaan (Terhubung via ID Foreign Key & Memiliki Kode Pos):
   - Kolom `kode_pos` ditambahkan pada tabel `companies` (`2026_09_08_163000_add_kode_pos_to_companies_table.php`).
   - Perusahaan berlokasi di 3 wilayah utama yang terhubung langsung lewat `province_id` dan `kabupaten_id`:
     - **DI Yogyakarta** (kode `34`) -> **Sleman** (kode `34.04`) (Kode Pos: 55281, 55581, 55284)
     - **DKI Jakarta** (kode `31`) -> **Kota Jakarta Pusat** (kode `31.71`) (Kode Pos: 10310, 12710, 12930, 10220)
     - **Aceh** (kode `11`) -> **Aceh Selatan** (kode `11.01`) (Kode Pos: 23715, 23711, 23714)
   - Seluruh data 10 alumni di-seed lengkap 100% rata mencakup data akademik, orang tua, skripsi, perusahaan, dan atasan.

STANDAR DOKUMENTASI KODE & ALUR HUBUNGAN BACKEND-INERTIA-FRONTEND (STEP 9)
1. Alur Penghubung (Lifecycle):
   - Browser akses URL -> Ditangkap di `routes/web.php` -> Memanggil Controller (Backend).
   - Controller melakukan query ke Database (Eloquent Model) -> Membungkus data -> Mengirim via `Inertia::render('Path/Komponen', ['props' => $data])`.
   - Inertia bertindak sebagai bridge (kurir JSON otomatis tanpa perlu membuat REST API manual).
   - Komponen Halaman Vue (`resources/js/Pages/...`) menangkap data via `defineProps` dan menampilkannya di template secara reaktif.

2. Hubungan Komponen Induk (Parent) dan Komponen Anak (Child):
   - Contoh pada Modul Profil Alumni:
     - `Index.vue` adalah Parent Component yang menerima `formData` dari `ProfilController.php`.
     - `Index.vue` membungkus data tersebut ke dalam `const form = useForm(props.formData)`.
     - Komponen anak (`FormAkademik.vue`, `FormPribadi.vue`, `FormOrangTua.vue`, `FormKarier.vue`) menerima variabel form tersebut melalui props operan: `<FormAkademik :form="form" />`.
     - Ketika tombol simpan di `Index.vue` ditekan, seluruh data dari keempat form anak dikirim serentak melalui `form.post('/alumni/profile')` ke `SimpanProfilController.php`.

3. Standar Komentar Kode:
   - Setiap Controller wajib mencantumkan komentar per-variabel untuk menjelaskan sumber data, tabel database yang digunakan, dan tujuan props yang dikirim ke Inertia.
   - Setiap Komponen Vue wajib mencantumkan komentar di bagian script yang merinci asal-usul props dan controller tujuan untuk setiap aksi (submit/logout).

4. Kuesioner Khusus (F13 & F17):
   - F13 (multiple_number): Input gaji format ribuan (suffix .000), total salary dihitung reaktif dan tersimpan otomatis di database (`answer_json.total` & `answer_text`).
   - F17 (komparasi berdampingan): Perbandingan Kolom A (Kemampuan Diri Saat Lulus) vs Kolom B (Kontribusi Kampus UKDW) dengan tabel rapi, skala 1 s/d 5, dan indikator komparasi minimalis tanpa overlap/clipping.

5. Sinkronisasi Dinamis Kuesioner Superadmin ke Alumni & Aturan Khusus F17:
   - **Mekanisme Real-Time**: Perubahan, penambahan, atau penghapusan butir pertanyaan serta opsi oleh Superadmin langsung termuat secara otomatis saat alumni membuka kuesioner tanpa perlu kompilasi ulang kode (karena `KuesionerController` membaca langsung dari database Eloquent).
   - **Deteksi Otomatis F17**: Frontend `Kuesioner.vue` mendeteksi keberadaan pertanyaan berawalan kode `F17-` untuk mengaktifkan antarmuka tabel komparasi dua sisi (A vs B).
   - **Aturan Berpasangan F17**: Superadmin harus menambahkan pertanyaan dalam jumlah genap (berpasangan A dan B):
     * Pertanyaan A: Menilai kemampuan diri sendiri (skala 1 s/d 5).
     * Pertanyaan B: Menilai kontribusi perguruan tinggi (skala 1 s/d 5).
     * Nama aspek diambil otomatis dari teks pertanyaan A sebelum tanda strip (`getCleanAspectName`).
   - **Pertanyaan Non-F17**: Secara dinamis dirender otomatis mengikuti nilai `type` pertanyaan (`single_choice`, `multiple_choice`, `text`, `number`, `multiple_number`, `textarea`).

6. Arsitektur Modular Modul Kelola Pertanyaan Superadmin:
   - **Modular Controller (Backend)**:
     * `DaftarPertanyaanController.php`: Menangani pemuatan data butir pertanyaan, section, opsi, dan mapping alur lompatan (*jump logic*).
     * `SimpanPertanyaanController.php`: Menangani operasi Create, Update, Delete, dan Reorder urutan posisi pertanyaan dalam section.
     * `KelolaOpsiController.php`: Menangani operasi Create, Update, Delete opsi jawaban beserta target branching `jump_to`.
   - **Modular Komponen (Frontend Vue)**:
     * `Navbar.vue`: Komponen navigasi terpusat (1 file) resmi SuperAdmin dengan branding UKDW, menu aktif, dan konfirmasi logout SweetAlert2.
     * `SectionTabs.vue`: Navigasi horizontal antar-section kuesioner dengan badge jumlah soal.
     * `QuestionCard.vue`: Kartu butir pertanyaan individu dengan kontrol aksi naik/turun urutan, edit/hapus soal, serta daftar opsi jawaban.
     * `QuestionModal.vue`: Modal popup dialog tambah & edit pertanyaan kuesioner.
     * `OptionModal.vue`: Modal popup dialog tambah & edit pilihan opsi jawaban dan target lompatan jump logic.
   - **Integrasi SweetAlert2**:
     * Konfirmasi hapus pertanyaan dan opsi jawaban dialog interaktif (`Swal.fire`).
     * Notifikasi keberhasilan dan error validasi dengan styling hijau emerald UKDW (`#005B3C`).

7. Arsitektur Kuesioner Khusus Program Studi & Harmonisasi Modul Admin Prodi (STEP 10):
   - **Pemisahan Database Kuesioner Prodi**:
     * Kolom `prodi_id` dihapus dari tabel kuesioner umum universitas (`questions`), sehingga kuesioner universitas murni untuk tingkat universitas.
     * Dibangun arsitektur tabel khusus prodi yang hierarkis dan independen:
       - `prodi_question_sections`: Menyimpan bagian/seksi kuesioner prodi (`id`, `prodi_id`, `title`, `description`, `order`).
       - `prodi_questions`: Menyimpan butir pertanyaan khusus prodi (`id`, `prodi_id`, `prodi_question_section_id`, `code`, `question_text`, `type`, `is_required`, `order`).
       - `prodi_question_options`: Menyimpan pilihan opsi jawaban butir pertanyaan prodi.
       - `prodi_responses`: Menyimpan respon dan jawaban alumni khusus kuesioner program studi (`alumni_id`, `prodi_question_id`, `answer_text`, `answer_json`).
   - **Auto-Prefill & Sinkronisasi Otomatis Data Akademik**:
     * 3 butir pertanyaan identitas utama (`Nama`, `NIM`, `Tahun Kelulusan` / kode `PSI-1-01`, `PSI-1-02`, `PSI-1-03`) secara otomatis diambil dari pangkalan Data Akademik (`data_akademiks`) alumni.
     * Di frontend alumni (`KuesionerProdi.vue`), field ditampilkan dalam format input khusus `readonly` dengan badge hijau resmi: *"✓ Data Akademik - Kolom ini diambil secara otomatis dari pangkalan Data Akademik resmi Anda"*, sehingga alumni tidak perlu mengetik ulang data diri.
     * Di backend (`KuesionerProdiController.php`), nilai secara otomatis diprefill pada `$initialAnswers` dan disinkronkan ke tabel `prodi_responses` (`updateOrCreate`), serta dilengkapi fallback otomatis saat formulir dikirimkan (`simpanJawaban`).
   - **Harmonisasi Modul Admin Prodi (Disetarakan dengan Super Admin)**:
     * `Dashboard.vue`: Header hijau solid UKDW `#0D542B`, 4 kartu KPI berlatar putih bersih (Total Alumni Prodi, Mengisi Tracer Univ, Mengisi Kuesioner Prodi, Profil Lengkap), 2 modul pintasan cepat, dan tabel aktivitas alumni.
     * `KelolaSectionProdiController.php` & `AdminProdi/Section/Index.vue`: Modul kelola seksi kuesioner prodi lengkap dengan CRUD, pencarian, dan reorder posisi urutan section (naik/turun).
     * `AdminProdi/Pertanyaan/Index.vue`: Modul kelola butir pertanyaan prodi yang bersih dari ikon 3D dan emoji (📁, 🗑️, ✎, ❓, ✕), digantikan dengan tombol teks elegan (`Edit`, `Hapus`, `+ Tambah Opsi`) dan badge rapi.
     * `DaftarAlumniProdiController.php` & `AdminProdi/Alumni/Index.vue`: Direktori alumni khusus prodi dengan filter pencarian, filter tahun lulus, dan tombol aksi "Lihat Detail" mengarah ke `/prodi/alumni/{id}`.
     * `AdminProdi/Alumni/Show.vue`: Halaman detail audit alumni prodi dengan 3 tab: Kuesioner Khusus Prodi, Kuesioner Tracer Universitas, dan Detail Profil Mahasiswa.
     * `SuperAdmin/Alumni/Show.vue`: Ditambahkan tab *"Kuesioner Khusus Prodi"* sehingga Super Admin juga dapat memantau dan mengaudit hasil kuesioner program studi setiap alumni secara komprehensif.

8. Arsitektur Modular Kuesioner Tracer Study Alumni (STEP 11):
   - **Pemisahan Komponen Monolitik (`resources/js/Pages/Alumni/Kuesioner.vue`)**:
     * Komponen induk `Kuesioner.vue` (sebelumnya 1.621 baris) didekomposisi menjadi arsitektur modular yang rapi di folder `resources/js/Pages/Alumni/Components/Kuesioner/`.
     * Komponen Anak Mandiri:
       1. `Navbar.vue`: Menangani header sticky atas, logo resmi UKDW, dan navigasi tombol Kembali ke Dashboard.
       2. `Stepper.vue`: Menangani navigasi tahapan kuesioner dengan bulatan angka 1..N, judul seksi, indikator centang selesai, dan auto-scroll horizontal.
       3. `Banner.vue`: Menangani kartu banner hijau judul bagian aktif ("Bagian X dari Y") dan instruksi pengisian.
       4. `TabelF17.vue`: Menangani tabel perbandingan kompetensi dual-matrix F17 (Kolom A: Kemampuan Diri vs Kolom B: Kontribusi Kampus) secara terisolasi.
       5. `KartuPertanyaan.vue`: Menangani kartu pertanyaan individual dan seluruh varian input (`rating_5`, `searchable_select`, `radio`, `checkbox`, `radio_input`, `multiple_number`, `number`, `text`, serta layout 2 kolom berdampingan F6 & F7).
       6. `Navigasi.vue`: Menangani tombol navigasi desktop melayang (< Kembali & > Lanjut/Selesai) serta fixed bottom bar di smartphone.
     * Komponen induk `Kuesioner.vue` kini ramping (~490 baris) dan bertindak murni sebagai orkestrator form Inertia, alur jump logic, dan persistensi sesi lokal (`localStorage`).

9. PANDUAN PENCARIAN CEPAT FILE & LOKASI STRUKTUR KODE (CHEATSHEET):
   - **Jika ingin mengedit Kuesioner Tracer Universitas (Alumni)**:
     * File Induk: `resources/js/Pages/Alumni/Kuesioner.vue`
     * Sub-Komponen: `resources/js/Pages/Alumni/Components/Kuesioner/`
       - Navbar: `Navbar.vue`
       - Stepper: `Stepper.vue`
       - Banner: `Banner.vue`
       - Tabel F17: `TabelF17.vue`
       - Input Soal: `KartuPertanyaan.vue`
       - Tombol Navigasi: `Navigasi.vue`
     * Controller: `app/Http/Controllers/Alumni/Kuesioner/KuesionerController.php`
   - **Jika ingin mengedit Kuesioner Khusus Program Studi (Alumni)**:
     * Halaman View: `resources/js/Pages/Alumni/KuesionerProdi.vue`
     * Controller: `app/Http/Controllers/Alumni/Kuesioner/KuesionerProdiController.php`
     * Seeder Instrumen: `database/seeders/ProdiQuestionnaireSeeder.php`
   - **Jika ingin mengedit Logika Sinkronisasi Data Akademik Otomatis**:
     * Service: `app/Services/Kuesioner/KuesionerSyncService.php` (method `syncProdiResponses()`)
   - **Jika ingin mengedit Modul Admin Program Studi**:
     * Dashboard: `resources/js/Pages/AdminProdi/Dashboard.vue`
     * Kelola Pertanyaan: `resources/js/Pages/AdminProdi/Pertanyaan/Index.vue`
     * Kelola Section: `resources/js/Pages/AdminProdi/Section/Index.vue`
     * Direktori Alumni: `resources/js/Pages/AdminProdi/Alumni/Index.vue`
     * Detail Audit Alumni: `resources/js/Pages/AdminProdi/Alumni/Show.vue`
     * Controllers: `app/Http/Controllers/AdminProdi/`
   - **Jika ingin mengedit Modul Super Admin**:
     * Kelola Kuesioner Univ: `resources/js/Pages/SuperAdmin/Pertanyaan/Index.vue`
     * Detail Audit Alumni: `resources/js/Pages/SuperAdmin/Alumni/Show.vue`
     * Controllers: `app/Http/Controllers/SuperAdmin/`

10. PETA STRUKTUR LENGKAP FILE & DIREKTORI PROYEK:
tracerstudy/
├── app/                                            # [BACKEND] Inti Logika Aplikasi (Laravel)
│   ├── Http/
│   │   ├── Controllers/
│   │   │   ├── AdminProdi/                         # Modul Administrator Program Studi (role: admin_prodi)
│   │   │   │   ├── Dashboard/DashboardController.php     # KPI & statistik respon alumni prodi
│   │   │   │   ├── KelolaAlumni/DaftarAlumniProdiController.php # Direktori & audit detail alumni prodi
│   │   │   │   └── KelolaPertanyaan/
│   │   │   │       ├── DaftarPertanyaanProdiController.php # Bank soal kuesioner prodi
│   │   │   │       ├── KelolaOpsiProdiController.php       # Opsi pilihan jawaban prodi
│   │   │   │       ├── KelolaSectionProdiController.php    # Bagian/seksi kuesioner prodi & urutan
│   │   │   │       └── SimpanPertanyaanProdiController.php  # Simpan & perbarui butir soal prodi
│   │   │   ├── Alumni/                             # Modul Alumni (role: alumni)
│   │   │   │   ├── Dashboard/DashboardController.php     # Ringkasan progres & kelengkapan profil
│   │   │   │   ├── Kuesioner/
│   │   │   │   │   ├── KuesionerController.php       # Kuesioner tracer study universitas (F1-F22)
│   │   │   │   │   ├── KuesionerProdiController.php  # Kuesioner khusus program studi
│   │   │   │   │   └── SimpanJawabanController.php   # Simpan jawaban kuesioner universitas
│   │   │   │   └── Profil/
│   │   │   │       ├── ProfilController.php        # Halaman kelola data diri alumni
│   │   │   │       └── SimpanProfilController.php  # Simpan perubahan profil pribadi, akademik, ortu, karier
│   │   │   ├── SuperAdmin/                         # Modul Super Admin (role: superadmin)
│   │   │   │   ├── Dashboard/DashboardController.php     # Metrik universitas & monitoring sistem
│   │   │   │   ├── KelolaAlumni/
│   │   │   │   │   ├── DaftarAlumniSuperAdminController.php # Direktori seluruh alumni universitas
│   │   │   │   │   └── DetailAlumniSuperAdminController.php # Audit jawaban kuesioner univ, prodi, profil
│   │   │   │   ├── KelolaPertanyaan/
│   │   │   │   │   ├── DaftarPertanyaanController.php  # Kelola bank instrumen universitas
│   │   │   │   │   ├── KelolaOpsiController.php        # Opsi jawaban & konfig jump_to
│   │   │   │   │   └── SimpanPertanyaanController.php  # Simpan & edit soal universitas
│   │   │   │   └── KelolaSection/
│   │   │   │       └── KelolaSectionController.php     # Kelola bagian/seksi kuesioner univ
│   │   │   ├── Otentikasi/                         # Modul Autentikasi Pengguna
│   │   │   │   ├── LoginController.php             # Login multi-role
│   │   │   │   └── UbahKataSandiController.php     # Form wajib ganti password awal
│   │   │   └── Tamu/
│   │   │       └── BerandaController.php           # Landing page publik & statistik alumni
│   │   └── Middleware/
│   │       ├── CheckMustChangePassword.php         # Interseptor jika user wajib ubah sandi
│   │       ├── HandleInertiaRequests.php           # Shared data Inertia SPA
│   │       └── RoleMiddleware.php                  # Pembatas hak akses role
│   ├── Models/
│   │   ├── User.php, Alumni.php, DataAkademik.php, DataOrangTua.php, Yudisium.php, Prodi.php
│   │   ├── Company.php, Atasan.php, Province.php, Kabupaten.php, Ump.php
│   │   ├── Kuesioner.php, KelompokPertanyaan.php, RefSubpertanyaan2021.php, RefSubpertanyaanDetil.php, Tracer.php, QuestionMapping.php
│   │   └── ProdiQuestionSection.php, ProdiQuestion.php, ProdiQuestionOption.php, ProdiResponse.php
│   └── Services/
│       ├── Kuesioner/KelengkapanTracerService.php  # Audit kelengkapan kuesioner & profil
│       ├── Kuesioner/KuesionerSyncService.php      # Auto-sync data akademik ke prodi_responses
│       └── LinkedIn/LinkedInService.php           # Sinkronisasi data profil LinkedIn
├── database/
│   ├── migrations/                                 # Seluruh DDL tabel master, kuesioner univ & prodi
│   └── seeders/                                    # Seeder database (User, Soal Univ, Soal Prodi SI & Filsafat)
├── resources/js/Pages/
│   ├── AdminProdi/                                 # Antarmuka Admin Program Studi
│   │   ├── Dashboard.vue                           # Dashboard KPI & aktivitas prodi
│   │   ├── Alumni/Index.vue & Alumni/Show.vue      # Direktori & detail audit 3-tab
│   │   ├── Pertanyaan/Index.vue                    # Kelola soal & opsi prodi
│   │   ├── Section/Index.vue                       # Kelola bagian/seksi prodi
│   │   └── Components/Navbar.vue                   # Header navigasi resmi admin prodi
│   ├── Alumni/                                     # Antarmuka Alumni
│   │   ├── Dashboard.vue                           # Dashboard kemajuan tracer alumni
│   │   ├── Kuesioner.vue                           # [INDUK] Form kuesioner universitas (~490 baris)
│   │   ├── KuesionerProdi.vue                      # Form kuesioner khusus prodi (auto-prefill)
│   │   ├── Components/Kuesioner/                   # [MODULAR] Navbar, Stepper, Banner, TabelF17, KartuPertanyaan, Navigasi
│   │   └── Profil/Index.vue & Components/          # Formulir data pribadi, akademik, orang tua, karier
│   ├── SuperAdmin/                                 # Antarmuka Super Admin
│   │   ├── Dashboard.vue, Alumni/Index.vue, Alumni/Show.vue
│   │   ├── Pertanyaan/Index.vue & Components/
│   │   ├── Section/Index.vue & Components/
│   │   └── Components/Navbar.vue
│   ├── AdminBiroTiga/                              # Antarmuka Admin Biro 3
│   ├── Otentikasi/                                 # Login.vue & UbahKataSandi.vue
│   └── Tamu/                                       # Beranda.vue (Landing Page Publik)
└── routes/
    ├── web.php                                     # Seluruh endpoint URL web aplikasi
    └── console.php                                 # Task scheduler console

================================================================================
ARSITEKTUR KUESIONER TRACER STUDY 2021 & TABEL TRACERS (UPDATE 2026-09-15)
================================================================================

1. STANDARISASI TABEL & MODEL DALAM BAHASA INDONESIA
   - `kuesioners` (Model: Kuesioner, Seeder: KuesionerSeeder)
     Menyimpan header kuesioner tracer study tingkat universitas (id, title, description, year, is_active).
   - `kelompok_pertanyaans` (Model: KelompokPertanyaan, Seeder: KelompokPertanyaanSeeder)
     Menyimpan kelompok/seksi instrumen kuesioner (id, kuesioner_id, kode_kelompok, title, description, order).
   - `ref_subpertanyaan2021` (Model: RefSubpertanyaan2021, Seeder: RefSubpertanyaan2021Seeder)
     Menyimpan butir subpertanyaan kuesioner 2021 (id, kelompok_pertanyaan_id, kelompok, kode_pertanyaan, subpertanyaan, type, keterangan, wajib, order).
     * Catatan: Kolom 'kelompok' bertipe CHAR(3) menyimpan kode induk kuesioner (seperti 'BIO', 'F1', 'F2', 'F17', 'F18'), BUKAN foreign key integer.
   - `ref_subpertanyaan_detil` (Model: RefSubpertanyaanDetil, Seeder: RefSubpertanyaanDetilSeeder)
     Menyimpan opsi pilihan jawaban dan alur percabangan (id, pertanyaan_id, kode_pertanyaan, kode_opsi, option_text, jump_to, order).
   - `tracers` (Model: Tracer)
     Menyimpan jawaban respon alumni tracer study universitas.
     Struktur kolom:
     - id: Primary Key (BIGINT)
     - alumni_id: FK ke tabel alumnis
     - question_id: FK ke tabel ref_subpertanyaan2021
     - nim: NIM alumni
     - kelompok: Kode instrumen kelompok ('BIO', 'F1', 'F2', 'F3', 'F4', 'F5', 'F6', 'F7', 'F8', 'F10', 'F11', 'F12', 'F14', 'F15', 'F16', 'F17', 'F18')
     - kode_pertanyaan: Kode subpertanyaan
     - subpertanyaan: Teks subpertanyaan selaras tabel induk
     - answer: Jawaban teks utama alumni (varchar/text)
     - answer_json: Struktur array/JSON untuk jawaban majemuk, matriks, atau checkbox
     - keterangan: Keterangan subpertanyaan atau judul kelompok
     - tahun_lulus: Tahun kelulusan dari data akademik
   - `v_question_mappings` (Database VIEW, Model: QuestionMapping, Seeder: QuestionMappingSeeder)
     Database VIEW yang menghubungkan kolom pangkalan profil alumni (data_akademiks, alumnis, companies, atasans) dengan kode pertanyaan kuesioner.

2. PEMISAHAN BIODATA BIO_TTL
   - BIO_TTL dipecah menjadi dua butir pertanyaan mandiri:
     1. BIO_TEMPAT_LAHIR: Mengambil data dari data_akademiks.tempat_lahir
     2. BIO_TANGGAL_LAHIR: Mengambil data dari data_akademiks.tanggal_lahir
   - Keduanya otomatis disinkronkan oleh KuesionerSyncService dan terdaftar di VIEW v_question_mappings.

3. SINKRONISASI OTOMATIS DATA PROFIL (KuesionerSyncService)
   Mencegah inkonsistensi data dengan menarik langsung data profil alumni ke tabel tracers:
   - F2E & F5B   -> Nama Perusahaan (companies.nama_perusahaan)
   - F2E1        -> Nama Atasan (atasans.nama)
   - F2E2        -> Nomor Telepon Atasan (atasans.telepon)
   - F2E3        -> Email Atasan (atasans.email)
   - F2F & F510  -> Alamat Perusahaan (alamat, kabupaten, provinsi, kode pos)
   - F2G & F5C   -> Posisi/Jabatan Alumni (alumnis.posisi_jabatan)
   - F2H & F5D   -> Skala Perusahaan (companies.skala)

4. REFACTORING LENGKAP MODEL KANONIKAL & STRUKTUR TABEL BARU
   - Model kanonikal utama: Kuesioner, KelompokPertanyaan, RefSubpertanyaan2021, RefSubpertanyaanDetil, Tracer, QuestionMapping
   - Seluruh wrapper model legacy dan seeder lama telah dihapus dan dimigrasikan secara penuh ke model kanonikal.
   - Pemetaan kolom terstandarisasi:
     - kode_pertanyaan: Kode butir pertanyaan (F1, F3, F13, dll.)
     - kode_opsi: Kode unik opsi pilihan jawaban
     - subpertanyaan: Teks butir pertanyaan
     - wajib: Status wajib diisi (boolean)
     - answer: Teks jawaban utama alumni di tabel tracers
     - answer_json: Struktur data majemuk/array/matriks di tabel tracers
   - Hasil: Seluruh controller, service, frontend Inertia/Vue, dan automated feature tests 100% lulus.

5. STANDARISASI PERTANYAAN GAJI F505 & KELENGKAPAN TEMPLATE INPUT VUE (UPDATE 2026-09-16)
   - Pertanyaan F505 ("Berapa rata-rata pendapatan anda per bulan ? (take home pay)?") diselaraskan ke tipe multiple_number.
   - Dilengkapi 3 opsi sub-rincian pendapatan pada ref_subpertanyaan_detil:
     1. F5051: "Dari Pekerjaan Utama"
     2. F5052: "Dari Lembur dan Tips"
     3. F5053: "Dari Pekerjaan Lainnya"
   - Logika backend (SimpanJawabanController) & frontend (KartuPertanyaan.vue) otomatis mengalikan input ribuan (.000) dengan 1000 ke Rupiah penuh, menghitung akumulasi total Take Home Pay, serta menyimpan format JSON ke answer_json dan teks ke answer.
   - Evaluasi kelengkapan kuesioner pada KelengkapanTracerService kini dinamis 100% membaca kolom 'wajib' (1/0) dari tabel ref_subpertanyaan2021.
   - Seluruh varian input kuesioner Google Forms telah tersedia template siap pakai di KartuPertanyaan.vue: text, textarea, number, single_choice/radio, radio_input, radio_text, multiple_choice/checkbox, dropdown, searchable_select, rating_5, multiple_number, date, time, dan file.




